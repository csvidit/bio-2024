import { Onest, Gantari } from 'next/font/google'

export const onest = Onest({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-onest',
})

export const gantari = Gantari({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-gantari',
})
